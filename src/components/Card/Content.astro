---
interface Props {
  title?: string;
  body?: string;
}

const { title, body } = Astro.props;
---

<>
  {title && <h2 class="text-xl f-bold m-0 z-20">{title}</h2>}
  {body && <p class="m-0 f-light text-base">{body}</p>}
  <slot />
</>
